// Tailwind v4 is a PostCSS plugin; there is no tailwind.config.js. Theme tokens are
// declared with @theme inside src/app/globals.css.
export default {
  plugins: { '@tailwindcss/postcss': {} },
};
